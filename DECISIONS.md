# DECISIONS — LLC Filing Funnel / Data Spine

Living register, same discipline as `florida-business-launchpad/GAPS.md`:
append immediately, in the same response a decision is made — never wait for
a summary or session end. **Never edit an existing entry.** A reversed or
changed decision is a new, appended entry that names which earlier entry it
supersedes; the original stays exactly as written.

*(Moved here from `florida-business-launchpad` — this is where the
data-spine work actually lives. Content unchanged from the original.)*

| # | Date | Decision | Context / Reason | Supersedes |
|---|---|---|---|---|
| 1 | 2026-08-11 | The durable data store is the **existing Supabase Postgres project** (`cedwebrzbpjwusbshdiu`), accessed via the `pg` driver directly — plain SQL, no Supabase JS client. Do not provision a second Postgres instance. | Supersedes the original "Postgres on Render preferred" framing from the initial data-spine request — the project already had this Supabase instance provisioned (if unused). | — |
| 2 | 2026-08-11 | Frontend (`florida-business-launchpad`) must never touch Supabase — no client-side reads/writes against it under any circumstance. | Only the server owns the database connection; keeps the anon/publishable key's blast radius irrelevant since the frontend has none of that surface at all. | — |
| 3 | 2026-08-11 | `DATABASE_URL` must use Supabase's **session pooler** (`aws-<region>.pooler.supabase.com:5432`) — not the direct connection (IPv6-only, unreliable from Render) and not the transaction pooler on port 6543 (disables prepared statements / session state, which breaks advisory-lock-based migrations and the planned `SELECT ... FOR UPDATE SKIP LOCKED` queue-claiming pattern). | — | — |
| 4 | 2026-08-11 | `pg` pool is capped at `max: 5` everywhere this project connects to Supabase. | Free-tier shared-CPU Micro instance has limited connection headroom; an exhausted pool presents as a hung request, not an obvious pool-size error. | — |
| 5 | 2026-08-11 | The new data-spine backend service lives at `llc-filing-intake/server/` (new directory in the existing worker/PDF-service repo) — not in `florida-business-launchpad`, not in a new standalone repo, and not inside the Cloudflare Worker itself. Deploys as its own Render Node Web Service, separate from the Flask/gunicorn PDF service. | Worker runtime (Cloudflare) can't hold persistent Postgres connections or run multi-statement transactions without a paid Hyperdrive add-on — incompatible with the transactional-enqueue + `SKIP LOCKED` queue design. Sharing the PDF service's process would let CPU-bound WeasyPrint renders starve transactional session writes. | — |
| 6 | 2026-08-11 | Migration direction (not yet built, but designed toward): Express becomes the single owner of all Zoho CRM writes. The Cloudflare Worker becomes a thin edge layer — Sunbiz proxy, routing — that forwards CRM-bound requests to Express and returns its response. Flask/gunicorn stays PDF-rendering only. | Keeps three services with clean, non-overlapping ownership instead of Zoho logic duplicated across runtimes. | — |
| 7 | 2026-08-11 | The Express service gets its **own, fresh Zoho OAuth client credentials** — not a copy of the Worker's existing `ZOHO_CLIENT_ID`/`SECRET`/`REFRESH_TOKEN`. | Keeps the two runtimes' Zoho access independently revocable during the migration window described in decision #6. | — |
| 8 | 2026-08-11 | Payment processor is **Stripe**. No alternatives to be evaluated. | — | — |
| 9 | 2026-08-11 | Deployment split: Claude builds code + deploy config (`render.yaml`, `.env.example`, `DEPLOY.md`) only. The user provisions and deploys via their own Render dashboard access. Claude never touches the Render account and never places a secret in any file. | Standing rule of this engagement, applied concretely to this build. | — |
| 10 | 2026-08-11 | `chore/remove-unused-supabase-client` merged to `main` in `florida-business-launchpad` (local merge; not pushed — the user pushes, per standing rule; this repo auto-publishes via Lovable on push, so pushing was held for separate confirmation). | Zero call sites confirmed before merge; independent of and safe ahead of the rest of the data-spine work. | — |
| 11 | 2026-08-11 | `.env` being tracked in `florida-business-launchpad`'s git history is fixed **going forward only** (`git rm --cached`, `.gitignore`, `.env.example`). Git history is **not** rewritten. | Two client-exposed-by-design "publishable" keys (Google Places, formerly Supabase anon) across multiple branches/remotes isn't judged worth a `filter-repo` history rewrite. | — |
| 12 | 2026-08-11 | `chore/untrack-env-file` (florida-business-launchpad) is held unmerged pending confirmation of how the Lovable-hosted production build actually sources `VITE_*` env vars (own dashboard config vs. reading the committed `.env`). | Merging blind risks silently breaking the live build if Lovable's build reads the committed file rather than its own separately-configured vars — unconfirmable from either repo alone. | — |
| 13 | 2026-08-11 | `GAPS.md` (florida-business-launchpad) is a living register for the whole project, not a one-time audit artifact: append on discovery, in the same response; never delete/renumber/rewrite a row; only Status/Commit SHA/Re-verified may be updated; re-verify every OPEN P0/P1 row against the current tree whenever `main` has moved since the last session, before any build work. | — | — |
| 14 | 2026-08-11 | `DECISIONS.md` follows the same append-only discipline as `GAPS.md`. | — | — |
| 15 | 2026-08-11 | `DECISIONS.md` and `GOVERNANCE.md` live in `llc-filing-intake`, not `florida-business-launchpad` — moved after being created in the wrong repo initially. `GAPS.md` stays in `florida-business-launchpad` (correct as originally placed — it tracks that repo's funnel defects). | Data-spine architecture/process decisions and standing rules belong with the backend they govern, not the frontend funnel repo. | Corrects the initial placement of `DECISIONS.md` in `florida-business-launchpad` (same content, no data lost — see that repo's git history for the original). |
| 16 | 2026-08-11 | No more than 3 unmerged branches per repo at any time. A 4th requires reporting the current backlog before creating it. | Six branches with nothing merged is how the audit went stale the first time. | — |
